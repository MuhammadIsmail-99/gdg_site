    git clone <repository-url>
    cd GDG-Wah-LandingPage
    ```

2.  **Option A: Local Development**
    If you have Node.js installed, you can use `serve` to run the site locally:
    ```bash
    npm install -g serve
    serve .
    